{
    "name": "gerges/sse-client",
    "description": "Server Sent Event client implementation",
    "type": "library",
    "license": "GPL-3.0-or-later",
    "autoload": {
        "psr-4": {
            "SseClient\\": "src/"
        }
    },
    "require": {
        "guzzlehttp/guzzle": "^7.8"
    },
        "scripts": {
        "test": [
            "phpstan analyse"
        ]
    },
    "config": {
        "platform": {
            "php": "8.1.29"
        }
    },
    "require-dev": {
        "phpstan/phpstan": "^1.12 || ^2.0"
    }
}
